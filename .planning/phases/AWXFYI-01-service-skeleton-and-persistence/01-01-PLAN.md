---
phase: "AWXFYI-01-service-skeleton-and-persistence"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["settings.gradle.kts", "build.gradle.kts", "gradlew", "gradlew.bat", "gradle/wrapper/gradle-wrapper.properties", "gradle/wrapper/gradle-wrapper.jar", ".gitignore", "src/main/kotlin/com/airwallexfyi/AirwallexFyiApplication.kt", "src/main/kotlin/com/airwallexfyi/config/AppProperties.kt", "src/main/resources/application.yml", "src/test/kotlin/com/airwallexfyi/AirwallexFyiApplicationTests.kt", "src/test/kotlin/com/airwallexfyi/config/AppPropertiesTest.kt"]
autonomous: true
requirements: ["OPS-01"]
user_setup: []
must_haves:
  truths:
    - "D-01: The repository contains a runnable Kotlin Spring Boot application scaffold."
    - "D-02: The Gradle build includes Spring Web, Validation, Data JDBC, Flyway, PostgreSQL, H2 test support, and Spring Boot Test."
    - "D-05: Database, OpenAI, Twilio, WhatsApp recipient, scheduler, dry-run, and admin token settings bind from environment-backed properties."
  artifacts:
    - path: "build.gradle.kts"
      provides: "Gradle Kotlin Spring Boot build"
      contains: "org.springframework.boot"
    - path: "src/main/kotlin/com/airwallexfyi/AirwallexFyiApplication.kt"
      provides: "Spring Boot application entrypoint"
      contains: "runApplication<AirwallexFyiApplication>"
    - path: "src/main/kotlin/com/airwallexfyi/config/AppProperties.kt"
      provides: "Typed application configuration"
      contains: "@ConfigurationProperties"
    - path: "src/main/resources/application.yml"
      provides: "Environment-backed defaults"
      contains: "airwallex-fyi"
  key_links:
    - from: "src/main/kotlin/com/airwallexfyi/AirwallexFyiApplication.kt"
      to: "src/main/kotlin/com/airwallexfyi/config/AppProperties.kt"
      via: "configuration properties scanning"
      pattern: "ConfigurationPropertiesScan"
---

<objective>
Create the runnable Kotlin Spring Boot base for Airwallex FYI.

Purpose: Establish a buildable, testable service skeleton that safely reads all Phase 1 operational configuration from environment-backed properties.
Output: Gradle project files, application entrypoint, typed configuration, safe local defaults, and config binding tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-RESEARCH.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Kotlin Spring Boot project</name>
  <files>settings.gradle.kts, build.gradle.kts, gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.properties, gradle/wrapper/gradle-wrapper.jar, .gitignore, src/main/kotlin/com/airwallexfyi/AirwallexFyiApplication.kt, src/test/kotlin/com/airwallexfyi/AirwallexFyiApplicationTests.kt</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md</read_first>
  <action>Create a Gradle Kotlin DSL Spring Boot application named `airwallex-fyi` with package `com.airwallexfyi`. First check local tooling with `java -version` and `gradle -v`. If `gradle` is unavailable, generate the project with the official Spring Initializr API or another approved Spring Boot generator that includes a real Gradle wrapper; request network/tool approval if needed, and do not handcraft a fake `gradle-wrapper.jar`. Include plugins for Kotlin JVM, Kotlin Spring, Spring Boot, and dependency management. Add dependencies for `spring-boot-starter-web`, `spring-boot-starter-validation`, `spring-boot-starter-jdbc`, `spring-boot-starter-actuator`, `spring-boot-starter-test`, `jackson-module-kotlin`, `flyway-core`, `flyway-database-postgresql`, PostgreSQL runtime, and H2 test runtime. Add an application class with `@SpringBootApplication`, `@ConfigurationPropertiesScan`, and `runApplication<AirwallexFyiApplication>(*args)`. Add a context-load smoke test.</action>
  <verify>On Windows run `.\gradlew.bat test`; on Unix run `./gradlew test`.</verify>
  <acceptance_criteria>
    - `settings.gradle.kts` sets `rootProject.name = "airwallex-fyi"`.
    - `build.gradle.kts` contains `org.springframework.boot` and `kotlin("plugin.spring")`.
    - `gradlew.bat`, `gradlew`, `gradle/wrapper/gradle-wrapper.properties`, and a real `gradle-wrapper.jar` exist; no placeholder wrapper jar is committed.
    - `AirwallexFyiApplication.kt` contains `@SpringBootApplication` and `runApplication<AirwallexFyiApplication>(*args)`.
    - `AirwallexFyiApplicationTests` loads the Spring context.
  </acceptance_criteria>
  <done>The application compiles and the context-load test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed environment-backed application properties</name>
  <files>src/main/kotlin/com/airwallexfyi/config/AppProperties.kt, src/main/resources/application.yml, src/test/kotlin/com/airwallexfyi/config/AppPropertiesTest.kt</files>
  <read_first>.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md</read_first>
  <action>Create `AppProperties` under the `airwallex-fyi` prefix with nested groups for OpenAI, Twilio, WhatsApp, scheduler, dry-run, and admin token. Configure Spring datasource values from `DATABASE_URL`, `DATABASE_USERNAME`, and `DATABASE_PASSWORD`. Use safe local defaults: H2 datasource, `dryRun=true`, scheduler disabled, and a development admin token only for local profiles/tests. Validate required production-facing values with bean validation annotations where practical, without requiring real OpenAI or Twilio secrets during tests.</action>
  <verify>Run `.\gradlew.bat test --tests "*AppPropertiesTest"` on Windows or `./gradlew test --tests "*AppPropertiesTest"` on Unix.</verify>
  <acceptance_criteria>
    - `application.yml` references `DATABASE_URL`, `OPENAI_API_KEY`, `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_FROM`, `WHATSAPP_TO`, `SCHEDULER_ENABLED`, `SCHEDULER_FIXED_DELAY`, `DRY_RUN`, and `ADMIN_TOKEN`.
    - `AppPropertiesTest` proves overridden properties bind into the typed object.
    - No real secret value is committed.
  </acceptance_criteria>
  <done>All Phase 1 OPS-01 settings bind from environment-backed properties.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\gradlew.bat test` passes on Windows or `./gradlew test` passes on Unix.
- [ ] `build.gradle.kts` includes the runtime and test dependencies needed by later Phase 1 plans.
- [ ] Configuration binding tests cover database, OpenAI, Twilio, WhatsApp, scheduler, dry-run, and admin token properties.
</verification>

<success_criteria>
- The Spring Boot Kotlin project builds and starts its test context locally.
- OPS-01 configuration placeholders exist and bind through typed properties.
- Safe local defaults do not call Airwallex, OpenAI, or Twilio.
</success_criteria>

<output>
After completion, create `.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-01-SUMMARY.md`.
</output>