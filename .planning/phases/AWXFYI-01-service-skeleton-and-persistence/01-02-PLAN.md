---
phase: "AWXFYI-01-service-skeleton-and-persistence"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["src/main/resources/db/migration/V1__initial_schema.sql", "src/main/kotlin/com/airwallexfyi/posts/PostRecord.kt", "src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt", "src/main/kotlin/com/airwallexfyi/summaries/SummaryRecord.kt", "src/main/kotlin/com/airwallexfyi/summaries/SummaryRepository.kt", "src/main/kotlin/com/airwallexfyi/notifications/NotificationAttemptRecord.kt", "src/main/kotlin/com/airwallexfyi/notifications/NotificationAttemptRepository.kt", "src/test/kotlin/com/airwallexfyi/persistence/PersistenceSchemaTest.kt"]
autonomous: true
requirements: ["STATE-01"]
user_setup: []
must_haves:
  truths:
    - "D-03: Persistence uses Spring Data JDBC repositories rather than JPA entities."
    - "D-04: Flyway creates posts, summaries, and notification attempt tables for future dedupe and notification state."
    - "STATE-01: The database can persist URL, source type, metadata, content hash, summary payload, notification status, and timestamps."
  artifacts:
    - path: "src/main/resources/db/migration/V1__initial_schema.sql"
      provides: "Initial Flyway database schema"
      contains: "CREATE TABLE posts"
    - path: "src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt"
      provides: "Spring Data JDBC access to post records"
      contains: "CrudRepository"
    - path: "src/test/kotlin/com/airwallexfyi/persistence/PersistenceSchemaTest.kt"
      provides: "Schema and repository slice coverage"
      contains: "@JdbcTest"
  key_links:
    - from: "src/main/resources/db/migration/V1__initial_schema.sql"
      to: "src/main/kotlin/com/airwallexfyi/posts/PostRecord.kt"
      via: "matching table and column names"
      pattern: "posts"
    - from: "src/main/kotlin/com/airwallexfyi/notifications/NotificationAttemptRecord.kt"
      to: "src/main/kotlin/com/airwallexfyi/posts/PostRecord.kt"
      via: "post id foreign key"
      pattern: "postId"
---

<objective>
Create the Phase 1 persistence foundation for monitor state.

Purpose: Give later source monitoring, summarization, and notification phases a durable schema with dedupe-friendly constraints.
Output: Flyway migration, Spring Data JDBC records/repositories, and a slice test proving insert/read behavior.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-RESEARCH.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the initial Flyway schema</name>
  <files>src/main/resources/db/migration/V1__initial_schema.sql</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md</read_first>
  <action>Create `posts`, `summaries`, and `notification_attempts` tables. `posts` must include id, url, source_type, title, description, author, published_at, sitemap_lastmod, discovered_at, content_hash, article_body, processing_status, created_at, and updated_at. `summaries` must include id, post_id, headline, summary_json, why_it_matters, tags_json, model, prompt_version, created_at, and updated_at. `notification_attempts` must include id, post_id, channel, recipient, status, provider_message_id, error_message, attempted_at, sent_at, created_at, and updated_at. Add unique constraints for `posts.url`, `summaries.post_id`, and `(post_id, channel, recipient)` notification attempts.</action>
  <verify>Run `.\gradlew.bat test --tests "*PersistenceSchemaTest"` after Task 3 is implemented.</verify>
  <acceptance_criteria>
    - Migration file contains `CREATE TABLE posts`, `CREATE TABLE summaries`, and `CREATE TABLE notification_attempts`.
    - `posts.url` is unique.
    - `summaries.post_id` references `posts(id)` and is unique.
    - `notification_attempts.post_id` references `posts(id)` and has a unique channel/recipient constraint.
  </acceptance_criteria>
  <done>Flyway can create the initial schema from a clean database.</done>
</task>

<task type="auto">
  <name>Task 2: Add Spring Data JDBC records and repositories</name>
  <files>src/main/kotlin/com/airwallexfyi/posts/PostRecord.kt, src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt, src/main/kotlin/com/airwallexfyi/summaries/SummaryRecord.kt, src/main/kotlin/com/airwallexfyi/summaries/SummaryRepository.kt, src/main/kotlin/com/airwallexfyi/notifications/NotificationAttemptRecord.kt, src/main/kotlin/com/airwallexfyi/notifications/NotificationAttemptRepository.kt</files>
  <read_first>src/main/resources/db/migration/V1__initial_schema.sql</read_first>
  <action>Create immutable Kotlin data classes annotated for Spring Data JDBC table mapping. Use `UUID` ids and `Instant` timestamps. Add repository interfaces extending `CrudRepository`, including `findByUrl(url: String)` for posts and `findTop20ByOrderByDiscoveredAtDesc()` or an equivalent recent-post query for admin use. Keep field names aligned with the Flyway schema and avoid JPA annotations.</action>
  <verify>Run `.\gradlew.bat test --tests "*PersistenceSchemaTest"` after Task 3 is implemented.</verify>
  <acceptance_criteria>
    - Repository files extend `CrudRepository`.
    - No file imports `jakarta.persistence` or `javax.persistence`.
    - Post repository can look up by URL and list recent posts.
  </acceptance_criteria>
  <done>Application code can persist and query Phase 1 state through Spring Data JDBC.</done>
</task>

<task type="auto">
  <name>Task 3: Prove schema and repository behavior with a slice test</name>
  <files>src/test/kotlin/com/airwallexfyi/persistence/PersistenceSchemaTest.kt</files>
  <read_first>src/main/resources/db/migration/V1__initial_schema.sql, src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt</read_first>
  <action>Add a `@JdbcTest` or equivalent slice test that runs Flyway against the test datasource, inserts a post record, reads it back by URL, and verifies the unique URL behavior. Use H2 PostgreSQL compatibility mode from the test configuration created in Plan 01. If H2 cannot support a SQL construct, simplify the schema to portable SQL rather than adding Docker as a Phase 1 requirement.</action>
  <verify>Run `.\gradlew.bat test --tests "*PersistenceSchemaTest"` on Windows or `./gradlew test --tests "*PersistenceSchemaTest"` on Unix.</verify>
  <acceptance_criteria>
    - Test inserts and reads a `PostRecord` through `PostRepository`.
    - Test verifies duplicate URLs are rejected or handled by the database/repository layer.
    - Full test suite still passes with `.\gradlew.bat test` or `./gradlew test`.
  </acceptance_criteria>
  <done>Persistence schema assumptions are covered by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\gradlew.bat test --tests "*PersistenceSchemaTest"` passes on Windows or `./gradlew test --tests "*PersistenceSchemaTest"` passes on Unix.
- [ ] `.\gradlew.bat test` or `./gradlew test` passes for the full suite.
- [ ] Flyway migration and Spring Data JDBC records agree on table and column names.
</verification>

<success_criteria>
- STATE-01 is represented by real schema and repository types.
- Phase 1 can persist post, summary, and notification status data without implementing later pipeline behavior.
- Unique constraints support future dedupe and notification suppression.
</success_criteria>

<output>
After completion, create `.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-02-SUMMARY.md`.
</output>