---
phase: "AWXFYI-01-service-skeleton-and-persistence"
plan: "03"
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: ["src/main/kotlin/com/airwallexfyi/admin/AdminTokenFilter.kt", "src/main/kotlin/com/airwallexfyi/admin/AdminController.kt", "src/main/kotlin/com/airwallexfyi/admin/AdminDtos.kt", "src/main/kotlin/com/airwallexfyi/monitor/MonitorRunService.kt", "src/test/kotlin/com/airwallexfyi/admin/AdminControllerSecurityTest.kt", "src/test/kotlin/com/airwallexfyi/admin/AdminControllerTest.kt"]
autonomous: true
requirements: ["OPS-01", "OPS-02"]
user_setup: []
must_haves:
  truths:
    - "D-05: Admin protection uses the configured admin token rather than a hard-coded secret."
    - "D-06: Protected admin endpoints exist for health, recent posts, and manual run-once."
    - "D-07: Manual run-once is a no-side-effect stub in Phase 1 and does not call Airwallex, OpenAI, or Twilio."
    - "OPS-02: Missing or wrong `X-Admin-Token` receives 401, while the configured token can call admin endpoints."
  artifacts:
    - path: "src/main/kotlin/com/airwallexfyi/admin/AdminTokenFilter.kt"
      provides: "Admin token route protection"
      contains: "X-Admin-Token"
    - path: "src/main/kotlin/com/airwallexfyi/admin/AdminController.kt"
      provides: "Admin health, recent posts, and run-once endpoints"
      contains: "/admin/run-once"
    - path: "src/main/kotlin/com/airwallexfyi/monitor/MonitorRunService.kt"
      provides: "Phase 1 run-once stub seam for later monitor pipeline"
      contains: "stub"
    - path: "src/test/kotlin/com/airwallexfyi/admin/AdminControllerSecurityTest.kt"
      provides: "Admin token protection tests"
      contains: "X-Admin-Token"
  key_links:
    - from: "src/main/kotlin/com/airwallexfyi/admin/AdminTokenFilter.kt"
      to: "src/main/kotlin/com/airwallexfyi/config/AppProperties.kt"
      via: "configured admin token"
      pattern: "admin"
    - from: "src/main/kotlin/com/airwallexfyi/admin/AdminController.kt"
      to: "src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt"
      via: "recent posts endpoint queries repository"
      pattern: "PostRepository"
    - from: "src/main/kotlin/com/airwallexfyi/admin/AdminController.kt"
      to: "src/main/kotlin/com/airwallexfyi/monitor/MonitorRunService.kt"
      via: "run-once endpoint delegates to stub service"
      pattern: "runOnce"
---

<objective>
Add the protected admin operator surface for the Phase 1 service.

Purpose: Prove the service can be operated locally through authenticated HTTP endpoints while keeping later external side effects stubbed.
Output: Admin token filter, health/recent/run-once endpoints, run-once stub service, and endpoint/security tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-RESEARCH.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-01-SUMMARY.md
@.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protect admin routes with the configured token</name>
  <files>src/main/kotlin/com/airwallexfyi/admin/AdminTokenFilter.kt, src/test/kotlin/com/airwallexfyi/admin/AdminControllerSecurityTest.kt</files>
  <read_first>src/main/kotlin/com/airwallexfyi/config/AppProperties.kt, .planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-CONTEXT.md</read_first>
  <action>Add a Spring filter or interceptor that applies only to `/admin/**`. It must compare the `X-Admin-Token` request header with `airwallex-fyi.admin.token` from `AppProperties`. Missing or wrong tokens return HTTP 401. Do not protect non-admin framework paths needed by tests unless intentionally configured. Add MockMvc tests for missing token, wrong token, and valid token.</action>
  <verify>Run `.\gradlew.bat test --tests "*AdminControllerSecurityTest"` on Windows or `./gradlew test --tests "*AdminControllerSecurityTest"` on Unix.</verify>
  <acceptance_criteria>
    - `AdminTokenFilter` or equivalent contains `X-Admin-Token`.
    - Tests prove missing and wrong tokens return 401.
    - Tests prove the configured token is accepted.
  </acceptance_criteria>
  <done>Admin routes are protected by typed configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin health, recent posts, and run-once endpoints</name>
  <files>src/main/kotlin/com/airwallexfyi/admin/AdminController.kt, src/main/kotlin/com/airwallexfyi/admin/AdminDtos.kt, src/main/kotlin/com/airwallexfyi/monitor/MonitorRunService.kt, src/test/kotlin/com/airwallexfyi/admin/AdminControllerTest.kt</files>
  <read_first>src/main/kotlin/com/airwallexfyi/posts/PostRepository.kt, src/main/kotlin/com/airwallexfyi/config/AppProperties.kt</read_first>
  <action>Create `GET /admin/health`, `GET /admin/posts/recent`, and `POST /admin/run-once`. Health should return service status plus safe operational flags such as dry-run and scheduler enabled. Recent posts should return the most recent persisted posts through `PostRepository` with a default limit of 20. Run-once should delegate to `MonitorRunService.runOnce()` and return a stub response such as `status: "stubbed"` or `status: "accepted"` without contacting Airwallex, OpenAI, or Twilio.</action>
  <verify>Run `.\gradlew.bat test --tests "*AdminControllerTest"` on Windows or `./gradlew test --tests "*AdminControllerTest"` on Unix.</verify>
  <acceptance_criteria>
    - `AdminController.kt` defines `/admin/health`, `/admin/posts/recent`, and `/admin/run-once` mappings.
    - `POST /admin/run-once` response clearly identifies the Phase 1 stub behavior.
    - Recent posts endpoint reads from `PostRepository`.
    - Tests cover successful authorized calls for all three endpoints.
  </acceptance_criteria>
  <done>The protected admin operator surface works end-to-end through MockMvc.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\gradlew.bat test --tests "*AdminController*"` passes on Windows or `./gradlew test --tests "*AdminController*"` passes on Unix.
- [ ] `.\gradlew.bat test` or `./gradlew test` passes for the full suite.
- [ ] Manual local smoke command is documented in the summary: start the app and call `/admin/health` with `X-Admin-Token`.
</verification>

<success_criteria>
- OPS-02 admin endpoints exist and are protected.
- OPS-01 admin-token configuration is actually used by route protection.
- Run-once is available as a no-side-effect stub for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/AWXFYI-01-service-skeleton-and-persistence/01-03-SUMMARY.md`.
</output>